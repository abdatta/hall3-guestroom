import { AvailabilityDirective } from './availability.directive';

describe('AvailabilityDirective', () => {
  it('should create an instance', () => {
    const directive = new AvailabilityDirective();
    expect(directive).toBeTruthy();
  });
});
